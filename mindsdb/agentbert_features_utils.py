import numpy as np
import pandas as pd


def preprocess_agent_bert_features_data(data):
    # Perform data preprocessing steps for AgentBERT features
    return data


def postprocess_agent_bert_features_predictions(predictions):
    # Perform postprocessing steps on AgentBERT features predictions
    return predictions
